extends main

block main
	+b("section").about#about: +i({
		title: file.about.title,
		featuresValue: file.about.featuresValue,
		featuresLabel: file.about.featuresLabel,
		btnMore: file.about.btnMore,
		btnMoreUrl: file.about.btnMoreUrl,
		text: file.about.text,
		date: site.date,
		location: site.location
	})

	+b("section").features#features: +i({
		title: file.features.title,
		list: file.features.list
	})

	//- +b("section").speakers#speakers: +i({
	//- 	title: file.speakers.title,
	//- 	persons: speakers,
	//- 	btnMore: file.speakers.btnMore,
	//- 	btnMoreLink: file.speakers.btnMoreLink
	//- })

	//- +b("section").program#program: +i({
	//- 	title: file.program.title,
	//- 	data: program
	//- })

	+b("section").contest#contest: +i({
		title: file.contest.title,
		text: file.contest.text,
		gifts: file.contest.gifts,
		btnMore: file.contest.btnMore,
		btnMoreLink: file.contest.btnMoreLink
	})

	.container
		hr.offset-0

	+b("section").news#news: +i({
		title: file.news.title,
		list: file.news.list,
		btnMore: file.news.btnMore,
		btnMoreLink: file.news.btnMoreLink
	})

	+b("section").tickets#tickets: +i({
		title: file.tickets.title,
		list: file.tickets.list
	})

	+b("section").sponsors#sponsors: +i({
		title: file.sponsors.title,
		list: sponsors
	})

	+b("section").map#contacts: +i({
		data: file.map.data,
		title: file.map.title,
		adress: site.location,
		mailLink: file.map.mailLink,
		mailLinkText: file.map.mailLinkText,
		times: file.map.times
	})

	+b("section").partners#partners: +i({
		title: file.partners.title,
		list: partners
	})
