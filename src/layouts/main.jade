extends root

block root
	h1.sr-only= site.title
	if file.intro
		+b("section").intro#intro: +i({
			title: file.intro.title,
			location: site.location,
			date: site.date,
			reg: site.reg,
			arrowUrl: file.intro.arrowUrl,
			language: file.language
		})
	+b("header").header#header: +i({ language: file.language, pageUrl: file.pageUrl })

	main.main
		block main
			!= content

	+b("footer").footer: +i({ language: file.language })

	if file.showModals
		+b.modals: +i({ speakersList: speakers, programList: program, language: file.language, speakersFullList: speakersFullList })

block fonts
	link(href="https://fonts.googleapis.com/css?family=Open+Sans:300,400,600&subset=cyrillic" rel="stylesheet")

block styles
	link(rel="stylesheet", href=asset("uisgcon.min.css"))

block scripts
	script(src=asset("jquery-1.11.0.min.js") defer)
	script(src=asset("uisgcon.min.js") defer)

block analytics
	script.
		(function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
		(i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
		m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
		})(window,document,'script','https://www.google-analytics.com/analytics.js','ga');

		ga('create', 'UA-68417909-1', 'auto');
		ga('send', 'pageview');
