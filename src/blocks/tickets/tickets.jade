.container
	+e("h2").title.main-title Варіанти квитків
	+e("ul").list
		if box
			each item, i in box
				+e("li").list-item
					+e.box
						+e.box-title!= item.title
						+e.box-price!= item.price
							sup!= item.currency
						if i === 2
							- classes = "active"
						else
							- classes = ""
						+e("a").box-order.btn.btn-default(class=classes href=item.orderLink)!= item.order
