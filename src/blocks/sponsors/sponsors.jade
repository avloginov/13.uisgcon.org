.container
	+e("h2").title.main-title!= title
	+e("ul").list
		if list
			each item in list
				+e("li").list-item
					+b("a").tile(href!=item.link target!=item.target): +i({ src: item.image, alt: item.alt, text: item.text })
